---
name: Выполнила Давлетбаева Илона
tags:
  - practic
  - python
---
### Практическая часть

#### Задание 1. Базовая структура (Умный дом)
***Решение
```mermaid
classDiagram
    class SmartLight {
        +int brightness
        +str color
        -bool is_on
        +turn_on()
        +turn_off()
        +set_color(new_color str)
    }
```

#### Задание 2. Инкапсуляция и Безопасность (Банковская карта)
***Решение
```mermaid
classDiagram
    class CreditCard {
        -str number
        -int cvc
        +str owner_name
        #float balance
        +pay(amount float)
        -check_pin(pin int) bool
    }
```

#### Задание 3. Наследование (Иерархия RPG)
***Решение
```mermaid
classDiagram
    class Character {
        +String name
        +move()
    }
    class Archer {
        +shoot()
    }
    class Knight {
        +attack_sword()
    }
    Character <|-- Archer
    Character <|-- Knight
```

#### Задание 4. Типы связей (Музыкальный плеер)
***Решение

```mermaid
classDiagram
    class PlayList {
        +String name
        -List~Song~ songs
        +add_song(song Song)
        +remove_song(song Song)
    }
    class Song {
        +String title
        +float duration
    }
    PlayList *-- Song
```
#### Задание 5. Комплексная система (Служба доставки)
***Решение

```mermaid
classDiagram
    class Order {
        +String status
        -List~Item~ items
        +assign_deliverer(deliverer Deliverer)
        +calculate_total()
    }
    class Item {
        +String name
        +float price
    }
    class Deliverer {
        +float speed
        +deliver(order Order)
    }
    class Courier {
        +call_client()
    }
    class Drone {
        +fly()
    }
    Deliverer <|-- Courier
    Deliverer <|-- Drone
   
    Order *-- Item 
    
    Order o-- Deliverer 
```
#### Задание 6
***Решение

```mermaid
classDiagram
    class User {
        +String username
        +String email
        -List~Post~ posts
        +create_post(content String) Post
        +get_posts()
    }
    class Post {
        +String content
        +DateTime created_at
        -List~Comment~ comments
        +add_comment(user User, text String) Comment
        +get_comments()
    }
    class Comment {
        +String text
        +DateTime created_at
    }
    User *-- Post : "создаёт"
    Post *-- Comment : "имеет"
```
